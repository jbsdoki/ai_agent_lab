# Lab FAQ (Standard)

KEYWORD: RAG-FAQ-BETA
DOC-ID: STD-RAG-002

## What is semantic search?

Semantic search finds passages by meaning, not exact keyword match.

## How do I reindex documents?

Run `python -m src.scripts.ingest_rag_corpus` after adding files under `database/standard/`.
